#include <stdio.h>
#include "main.h"

/**
 * positive_or_negative: function to test
 * main - Entry point
 * Return: 0 (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
