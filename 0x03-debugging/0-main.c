#include "main.h"
#include <stdio.h>

/**
 * positive_or_negative: function to test
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
	positive_or_negative(0);

	return (0);
}
